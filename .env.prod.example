# ─── PRODUCTION environment ──────────────────────────────────────────────────
# Copy to .env.prod and change EVERY secret before deploying.

# Database (use a strong password)
POSTGRES_USER=nursery
POSTGRES_PASSWORD=CHANGE_ME_strong_db_password
POSTGRES_DB=nursery_analytics
POSTGRES_HOST=db
POSTGRES_PORT=5432

# ─── Host ports (coexist with software already on the server) ─────────────────
# The app is published here (NOT 80) so a host NGINX keeps owning 80/443.
# Point your host NGINX at http://127.0.0.1:8080  (see DEPLOYMENT.md).
FRONTEND_HOST_PORT=8080
# Our Postgres is published on loopback only at this port, so a host Postgres on
# 5432 is untouched. Reach it via pgAdmin on the server or an SSH tunnel.
DB_HOST_PORT=15432

# Security — MUST regenerate:  openssl rand -hex 32
SECRET_KEY=CHANGE_ME_run_openssl_rand_hex_32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# First admin (created only if you run the seed/bootstrap step)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_ME_strong_admin_password

# App
ENVIRONMENT=production
RUN_MIGRATIONS_ON_STARTUP=false
SEED_ON_STARTUP=false
# Same-origin via nginx proxy, so CORS can be left empty (or set your domain)
BACKEND_CORS_ORIGINS=
